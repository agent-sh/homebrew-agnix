# homebrew-agnix

Homebrew tap for [agnix](https://github.com/agent-sh/agnix) — AI agent configuration linter.

## Install

```bash
brew tap agent-sh/agnix
brew install agnix
```

## Update

```bash
brew update
brew upgrade agnix
```

## How it works

The formula is automatically updated when a new agnix release is published via GitHub Actions.
